Frontend demo using Parcel + React.

Run locally:
```powershell
cd frontend
npm install
npm start
```

Notes:
- This is a minimal demo that posts to `/search` on the same origin. Configure a proxy in development or run Node API gateway on same host/port.
